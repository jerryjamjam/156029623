#include <cs50.h>
#include <stdio.h>

float average(int length, int array[])

int main (void)
{
    const int N = 3;
    int scores[N]
    for (int i = 0, i < N, i++)
    {
        scores[i] = getint("Score: ");
    }

    printf("Score: %f\n", scores[i] / 3)
}

float average(int length, int array[])
{
    int sum = 0;
    for (int i =0, i < length, i++)
    {
        sum += array[i];
    }
    return sum / 
}
